options:
  basic:
    packages:
    - autoconf
    - automake
    - make
    - gcc
    - git
  git-deploy:
    target: "/srv/presentation"
includes:
- layer:basic
- layer:git-deploy
- layer:nginx
- interface:http
repo: "https://github.com/jamesbeedy/layer-present.git"
