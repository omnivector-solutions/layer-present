"update-presentation":
  "description": "Update to a commit sha or tip of branch"
  "params":
    "commit-or-branch":
      "type": "string"
      "description": "The commit sha or branch name to update to"
